import styled from 'styled-components';
import { constants } from '../../utils/constants/globalStyles';

const Input = styled.input`
	border-bottom: 2px solid ${constants.colors.goldAccent}
	display: block
	padding: 1em
	width: 100%

	
`

export default Input;
